# Frontend -> Backend wiring
# Set this to the FastAPI backend origin (no trailing slash).
# Example (local): http://localhost:3001
# Example (deployed): https://your-backend.example.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
